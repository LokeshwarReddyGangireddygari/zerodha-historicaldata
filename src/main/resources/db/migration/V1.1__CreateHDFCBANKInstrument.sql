CREATE TABLE HDFCBANK(
                        day DATE  NOT NULL PRIMARY KEY ,
                        open real  NOT NULL,
                        high real NOT NULL,
                        low real NOT NULL,
                        close real NOT NULL,
                        volume integer  NOT NULL,
                        oi integer  NOT NULL
);
